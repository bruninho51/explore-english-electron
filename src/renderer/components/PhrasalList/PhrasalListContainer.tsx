import styled from 'styled-components';
import { Section } from './Section';

export const PhrasalListContainer = styled(Section)`
  width: 40%;
  display: block;
  height: 100%;
  overflow: hidden;
`;
